
class Child(var x: Int) {
    def myf() : Int =  x ;
}

class Main () extends IO() {
    {

        var p : Child = new Child(12);
        var i : Int = p.myf();
        //result should be 45
        this.out(i)
    };
}